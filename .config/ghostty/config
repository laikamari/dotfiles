font-family = "IBM Plex Mono"
font-size = "16"

font-style = "SemiBold"
font-style-bold = "Bold"
font-style-italic = "SemiBold Italic"
font-style-bold-italic = "Bold Italic

theme = Ayu

cursor-style = bar

window-padding-x = 16
window-padding-y = 16

# Always save and restore window state (position, size, tabs, splits)
window-save-state = always

# Enable shell integration for working directory preservation
shell-integration = detect
shell-integration-features = no-sudo,cursor,title,ssh-env,ssh-terminfo

# Control auto-update behavior to avoid surprise restarts
auto-update = check

